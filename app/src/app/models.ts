// This is a file with types that are returned from the API
// it is just helpful for typing each variable

export class Area {
    id: number;
    name: string;
    img: string;
}

export class Feed {
    id: number;
    name: string;
    count: number;
    url: string;
}
